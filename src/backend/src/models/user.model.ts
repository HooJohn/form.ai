import { UserProfile } from '@backend/common/types';

// This is a placeholder for a database model.
// In a real application, this would be a Mongoose, Prisma, or TypeORM schema.
export type UserModel = UserProfile;
